# Docker Project

E-commerce backend using:

- TypeScript
- Prisma
- PostgreSQL
- Docker

## Setup

Install dependencies:

```bash
npm install
```

Run development server:

```bash
npm run dev
```

Prisma visual databas:

```bash
npm prisma studio
```

Run Docker:

```bash
docker compose up --build
```
